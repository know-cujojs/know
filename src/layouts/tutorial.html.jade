---
layout: default
---

article.tutorial
  h1= document.title
  .post-content!= content

footer
  if document.relatedDocuments && document.relatedDocuments.length
    section#related
      h3 Related Links
      nav.linklist
        each doc in document.relatedDocuments
          li
            span= formatDate(doc.date, 'YYYY-MM-DD')
            |  &raquo;&nbsp;
            a(href=doc.url)= doc.title
